[project]
name = "session-class"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jahanzaib Tayyab", email = "jtayyab204@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "openai-agents>=0.4.2",
    "python-dotenv>=1.2.1",
    "rich>=14.2.0",
]

[project.scripts]
session-class = "session_class:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
